<%= form_with(model: @micropost, local: true) do |f| %>
  <%= render 'shared/error_messages', object: f.object %>
  <div class="field">
    <%= f.text_area :content, placeholder: "Что нового?..." %>
  </div>
  <%= f.submit "Опубликовать", class: "btn btn-primary" %>
<% end %>