<%= form_with(model: current_user.active_relationships.find_by(followed_id: @user.id),
            method: :delete) do |f| %>
  <%# Добавлен класс btn-unfollow для кастомной стилизации в CSS %>
  <%= f.submit "Unfollow", class: "row btn btn-unfollow" %>
<% end %>